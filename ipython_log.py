# IPython log file

get_ipython().magic(u'pwd ')
get_ipython().magic(u'ls ')
get_ipython().magic(u'cd nanog66/')
get_ipython().magic(u'ls ')
with open('syslog.txt', 'r') as f:
    syslog_line = f.readlines()
    for line in syslog_line:
        print line
        
SYSLOG_FILE = 'syslog.txt'
import re
import paramiko
syslog_line
TIMESTAP_RE = r'(\s+\d+\s+\d+).*
TIMESTAP_RE = r'(\s+\d+\s+\d+).*'
import re
for line in syslog_line:
    if re.match(TIMESTAMP_RE, line):
        print re.match(TIMESTAMP_RE, line)
        
for line in syslog_line:
    if re.match(TIMESTAMP_RE, line):
        print re.match(TIMESTAP_RE, line)
        
TIMESTAMP_RE = r'(\s+\d+\s+\d+).*
TIMESTAMP_RE = r'(\s+\d+\s+\d+).*'
for line in syslog_line:
    if re.match(TIMESTAMP_RE, line):
        print re.match(TIMESTAMP_RE, line)
        
line
date = '2015 May 18'
DATESTAMP_RE = r'(\d+\s\w+\d+)'
matched = re.match(DATESTAMP_RE, date)
matched
print matched.groups()
print matched
DATESTAMP_RE = r'(\d+\s\w+\s\d+)'
matched = re.match(DATESTAMP_RE, date)
matched
print matched
print matched.group()
print matched.groups()
print matched.group(1)
print matched.group(0)
print matched.group(2)
print matched.group(0)
time = '12:43:15 '
TIMESTAMP_RE = r'(\d+:\d+:\d+)S'
matched_timestamp = re.match(TIMESTAMP_RE, time)
matched_timestamp
TIMESTAMP_RE = r'(\d+:\d+:\d+)S+'
matched_timestamp = re.match(TIMESTAMP_RE, time)
matched_timestamp
print matched_timestamp
TIMESTAMP_RE = r'(\d+:\d+:\d+)\S+'
matched_timestamp = re.match(TIMESTAMP_RE, time)
print matched_timestamp
print matched_timestamp.group()
print line
get_ipython().magic(u'clear ')
DATESTAMP_RE = r’(\d+\s\w+\s\d+)’
SPACE_RE = r’\s+
TIMESTAMP_RE = r’(\d+:\d+:\d+)’
DEVICE_RE = r’(\S+)
ERROR_CODE_RE = r’%(.*):’
ERROR_MSG_RE = r’(.*)’
DATESTAMP_RE = r’(\d+\s\w+\s\d+)’
DATESTAMP_RE = r'(\d+\s\w+\s\d+)'
SPACE_RE = r'\s+'
TIMESTAMP_RE = r'(\d+:\d+:\d+)'
DEVICE_RE = r'(\S+)'
ERROR_CODE_RE = r'%(.*):'
ERROR_MSG_RE = r'(.*)'
SYSLOG_ RE = DATESTAMP_RE + SPACE_RE + TIMESTAMP_RE + SPACE_RE + DEVICE_RE + SPACE_RE + ERROR_CODE_RE + SPACE_RE + ERROR_MSG_RE
SYSLOG_ RE = DATESTAMP_RE + SPACE_RE + TIMESTAMP_RE + SPACE_RE + DEVICE_RE + SPACE_RE
SYSLOG_ RE = DATESTAMP_RE + SPACE_RE + TIMESTAMP_RE + SPACE_RE + DEVICE_RE + SPACE_RE + ERROR_CODE_RE + SPACE_RE + ERROR_MSG_RE
SYSLOG_ RE = DATESTAMP_RE + SPACE_RE + TIMESTAMP_RE + SPACE_RE + DEVICE_RE + SPACE_RE \ + ERROR_CODE_RE + SPACE_RE + ERROR_MSG_RE
SYSLOG_ RE = DATESTAMP_RE + SPACE_RE + TIMESTAMP_RE + SPACE_RE + DEVICE_RE + SPACE_RE \ + ERROR_CODE_RE + SPACE_RE + ERROR_MSG_RE
SYSLOG_ RE = DATESTAMP_RE + SPACE_RE + TIMESTAMP_RE + SPACE_RE + DEVICE_RE+ SPACE_RE + ERROR_CODE_RE + SPACE_RE + ERROR_MSG_RE
SYSLOG_ RE = DATESTAMP_RE + SPACE_RE + TIMESTAMP_RE + SPACE_RE + DEVICE_RE + SPACE_RE + ERROR_CODE_RE + SPACE_RE + ERROR_MSG_RE
SYSLOG_RE = DATESTAMP_RE + SPACE_RE + TIMESTAMP_RE + SPACE_RE + DEVICE_RE + SPACE_RE + ERROR_CODE_RE + SPACE_RE + ERROR_MSG_RE
print SYSLOG_RE
get_ipython().magic(u'ls ')
SYSLOG
SYSLOG = 'syslog.txt'
with open('SYSLOG', 'r') as f:
    line = f.readlines()
    
get_ipython().magic(u'pwd ')
get_ipython().magic(u'ls ')
SYSLOG = 'syslog.txt'
with open(SYSLOG, 'r') as f:
    log_line = f.readlines()
    
for line in log_line:
    if matched = re.match(SYSLOG_RE, line)
for line in log_line:
    if matched = re.match(SYSLOG_RE, line):
        print matched.groups()
        

for line in log_line:
    if matched = re.match(SYSLOG_RE, line):
        print matched.groups()
        
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if matched:
        print matched.groups()
        
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if matched:
        print matched.groups(1)
        
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if matched:
        print matched.group()
        
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if matched:
        print matched.group(1)
        
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if matched:
        print matched.group(2)
        
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if matched:
        print matched.group(3)
        
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if matched:
        print matched.group(4)
        
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if matched:
        print matched.group(5)
        
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if matched:
        print matched.group(6)
        
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if not matched:
        print matched.group(5)
        
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if not matched:
        print matched.group(5)
        
for line in log_line:
        matched = re.match(SYSLOG_RE, line)
        if not matched:
               continue:
                print matched.group(3), matched.group(5)
            
for line in log_line:
        matched = re.match(SYSLOG_RE, line)
        if not matched:
               continue:
        print matched.group(3), matched.group(5)
        
python
google.com
vi
get_ipython().magic(u'edit')
get_ipython().magic(u'edit')
get_ipython().magic(u'edit nano')
for line in log_line:
        matched = re.match(SYSLOG_RE, line)
        if not matched:
               continue:
        datestamp, timestamp, device, error_code, error_msg = matched.groups()
        
for line in log_line:
        matched = re.match(SYSLOG_RE, line)
        if not matched:
               continue
        datestamp, timestamp, device, error_code, error_msg = matched.groups()
    
datestamp
timestamp
device
error_code
error_msg
for line in log_line:
    print line
    
if 'ETHPORT-5-IF_DOWN_INTERFACE_REMOVED' in error_code:
    print('Found a known error on {0}'
          '-- attempting remediation.'
          .format(device))
    
for line in log_line:
        matched = re.match(SYSLOG_RE, line)
        if not matched:
               continue:
                datestamp, timestamp, device, error_code, error_msg = matched.groups()
            
if 'ETHPORT-5-IF_DOWN_INTERFACE_REMOVED' in error_code:
       print('Found a known error on {0} -- attempting remediation'.format(device))
    
get_ipython().magic(u'logstart')
import re
SYSLOG = 'syslog.txt'
SYSLOG_RE = DATESTAMP_RE + SPACE_RE + TIMESTAMP_RE + SPACE_RE + DEVICE_RE + SPACE_RE + ERROR_CODE_RE + SPACE_RE + ERROR_MSG_RE
with open(SYSLOG, 'r') as f:
    log_line = f.readlines()
    
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if not matched:
        continue
    datestamp, timestamp, device, error_code, error_msg = matched.groups()
    
if 'ETHPORT-5-IF_DOWN_INTERFACE_REMOVED' in error_code:
    print('Found a known error on {0} -- attempting remediation'.format(device))
    
error_code
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
        continue
get_ipython().magic(u'paste')
get_ipython().magic(u'paste')
get_ipython().magic(u'cpaste')
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if not matched:
       continue:
    datestamp, timestamp, device, error_code, error_msg = matched.groups()
    if 'ETHPORT-5-IF_DOWN_INTERFACE_REMOVED' in error_code:
       print('Found a known error on {0} -- attempting remediation'.format(device))
get_ipython().magic(u'cpaste')
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if not matched:
       continue:
    datestamp, timestamp, device, error_code, error_msg = matched.groups()
    if 'ETHPORT-5-IF_DOWN_INTERFACE_REMOVED' in error_code:
       print('Found a known error on {0} -- attempting remediation'.format(device))
get_ipython().magic(u'cpaste')
for line in log_line:
    matched = re.match(SYSLOG_RE, line)
    if not matched:
       continue
    datestamp, timestamp, device, error_code, error_msg = matched.groups()
    if 'ETHPORT-5-IF_DOWN_INTERFACE_REMOVED' in error_code:
       print('Found a known error on {0} -- attempting remediation'.format(device))
for line in log_line:
    :    matched = re.match(SYSLOG_RE, line)
    :    if not matched:
        :       continue
        :    datestamp, timestamp, device, error_code, error_msg = matched.groups()
        :    if 'ETHPORT-5-IF_DOWN_INTERFACE_REMOVED' in error_code:
            :       print('Found a known error on {0} -- attempting remediation'.format(device))
            
get_ipython().magic(u'quit')
get_ipython().magic(u'exit')
get_ipython().magic(u'quit')
get_ipython().magic(u'logoff')
